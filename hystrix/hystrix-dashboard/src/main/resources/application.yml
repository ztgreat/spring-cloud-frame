spring:
  application:
    name: eureka-consumer-feign-hystrix
server:
  port: 8083

feign:
  hystrix:
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
